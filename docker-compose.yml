version: '3.8'

services:
  postgres_db:
    image: postgres:15-alpine
    container_name: faims_postgres
    restart: always
    environment:
      POSTGRES_USER: faims_admin
      POSTGRES_PASSWORD: faims_password
      POSTGRES_DB: faims_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: